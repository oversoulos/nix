{ ... }:
{
  oversoul.modeSpecs.spiraling = {
    description = "Deep research workspace with timeboxed sessions";
    tools = [ "brave" "obsidian" "ghostty" "btop" ];
    hotkeys = [ "Ctrl+r Record" "Ctrl+f Fact check" "$mod+Shift+s Start timer" ];
  };
}
